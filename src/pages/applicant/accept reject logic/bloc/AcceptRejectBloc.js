// navigation diambil dari route navigation di folder routes
const AcceptRejectBloc = (navigation) => {
  let { navigateTo, paramsNav } = navigation();
  let params = paramsNav();

  return <div></div>;
};

export default AcceptRejectBloc;
